In order to make and run the application: "make app"
- This will also clean everything else, and it'll run the db creation script "dbtest.py"

The block nested function is at the bottom of schema.c, although it won't work.

